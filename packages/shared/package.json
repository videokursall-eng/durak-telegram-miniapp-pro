{
  "name": "@durak/shared",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "echo shared has no dev",
    "lint": "echo todo",
    "test": "tsc && node --test tests/**/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}