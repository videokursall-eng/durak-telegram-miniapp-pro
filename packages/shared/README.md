# packages/shared — типы и движок правил

Сюда выносим:
- типы карт и состояния (`Card`, `GameState`, `PlayerState`)
- действия (`GameAction`)
- чистую функцию `applyAction(state, action)` + валидаторы
- тесты на правила (самое важное для “Дурака”)

Первые задачи — в `prompts/cursor/03_SHARED_RULES_ENGINE.md`.
