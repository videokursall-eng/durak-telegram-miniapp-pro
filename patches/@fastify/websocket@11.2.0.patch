diff --git a/index.js b/index.js
--- a/index.js
+++ b/index.js
@@ -145,10 +145,13 @@ function fastifyWebsocket (fastify, opts, next) {
   })
 
   fastify.addHook('onResponse', (request, _reply, done) => {
-    if (request.ws) {
-      request.raw[kWs].destroy()
-    }
+    // Do NOT destroy the socket for WebSocket upgrades: the connection must stay open.
+    // See durak-telegram-miniapp-pro: plugin was closing WS right after handler return.
+    // if (request.ws) {
+    //   request.raw[kWs].destroy()
+    // }
     done()
   })
